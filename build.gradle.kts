buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath(androidGradlePlugin)
        classpath(kotlinGradlePlugin)
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
